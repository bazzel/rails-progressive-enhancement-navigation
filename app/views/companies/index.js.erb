var htmlString = '<%= j render(partial: 'results', object: @companies) %>';

<%- if @companies.first_page? %>
  document.querySelector('.companies').innerHTML = htmlString;
<%- else %>
  document.querySelector('.load-more').outerHTML = htmlString;
<%- end %>